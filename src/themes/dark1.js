import theme1 from "./theme1";
import { extend } from "./utils";

const dark1 = extend(theme1, {
  textPrimary: "#d4d4d4",
  textSecondary: "#131616",
  bgPrimary: "#1B1D1D",
  bgSecondary: "#212121",
});

export default dark1;
